<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.house_committee">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.House_Committee">
        <activity android:name=".All_payments_in_the_building"></activity>
        <activity android:name=".Details_of_tenant_payments" />
        <activity android:name=".register_Customer" />
        <activity android:name=".register_HouseCommittee" />
        <activity android:name=".HouseCommittee_Home" />
        <activity android:name=".Login" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>